# Server
server.port=8082
spring.application.name=demo

# Database
spring.datasource.url=jdbc:mariadb://localhost:3306/weather_envirotrack?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# JWT
app.jwt.secret=YourSuperSecretKeyForJWTGenerationAtLeast512BitsLongChangeThisToASecureKeyAndMakeItEvenLongerToEnsureSecurityRequirementsAreMet123456789
app.jwt.expiration=86400000 

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.com.backend.demo=DEBUG

# Configuration des timestamps
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Format des dates dans les réponses JSON
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# API Keys
openweather.api.key=f7b4113173d775dbd8feadf99dfac5d5
weatherapi.api.key=4825388d91bb4af5ae9191442251305

# MQTT Configuration
mqtt.broker.url=tcp://localhost:1883
mqtt.client.id=spring-server-${random.uuid}
mqtt.topic.airquality=capteurs/qualite_air
mqtt.qos=1

# Simulation
sensor.simulation.enabled=true
sensor.simulation.interval=60000

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Logging Configuration
logging.charset.console=UTF-8
logging.file.name=logs/app.log
logging.level.root=INFO
logging.level.org.eclipse.paho.client.mqttv3=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level [%15.15t] %-40.40logger{39} : %msg%n


# Configuration WebSocket/MQTT
spring.websocket.allowed-origins=http://localhost:4200,ws://localhost:4200
spring.mvc.async.request-timeout=-1

# Activer les endpoints WebSocket
spring.websocket.endpoint=/mqtt
spring.websocket.path=/ws

# Configuration WebSocket
spring.websocket.max-text-message-size=8192
spring.websocket.max-binary-message-size=8192


# Email configuration for Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=weatherenvirotrack@gmail.com
spring.mail.password=umas sngd ihna oiyb  

# SMTP properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Token expiration
app.token.expiration.hours=24
app.frontend.url=http://localhost:4200

# Cross-Origin Resource Sharing (CORS) configuration
spring.web.resources.add-mappings=true

# RestTemplate configuration
spring.main.allow-bean-definition-overriding=true

# Spring Boot Actuator
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true